# rules we should flat out ignore
IGNOREZERO:this is the verb-checker rule,carry out blowbying,carry out tricktriping
# rules that don't need a "rule fails" because we can always try them
# flip the big bag, leave the wet wood, guess "bury bile" as final command
NOFAIL:vc-big-bag,vc-get-good,vc-bury-bile
#IGNOREMULT: for when we ignore multiple rule succeeds/rule fails
;

